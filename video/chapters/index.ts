import type { ChapterData } from "../types";
import { kapitel02 } from "./kapitel02";

/**
 * Alle Kapitel des Projekts. Jeder Eintrag wird automatisch als eigene
 * Composition im Studio angezeigt und ist einzeln renderbar.
 */
export const chapters: ChapterData[] = [kapitel02];
